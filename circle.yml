machine:
  environment:
    GYM_CODE_SIGNING_IDENTITY: "iPhone Distribution: TOKOPEDIA PT (YQP4A2M94J)"
    XCODE_SCHEME: "Tokopedia"
  xcode:
    version: 8.3.2

deployment:
  beta_distribution:
    branch: master
    commands:
      - fastlane beta
